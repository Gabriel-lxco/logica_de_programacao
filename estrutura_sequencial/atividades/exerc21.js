let numero

numero = parseFloat(prompt("Digite um número positivo ou negativo: "))

if (numero > 0) {
    document.write(`O seu número é positivo`)
} else {
    document.write(`O seu número é negativo`)
}